/**
 * Deprecated: comment.loader contained duplicated single-entity loaders.
 * Single-entity lookups should use canonical loaders:
 *  - `UserLoader` for users
 *  - `PostLoader` for posts
 *
 * This file intentionally contains no active loaders to avoid duplication.
 */

export {}; // keep module boundary
